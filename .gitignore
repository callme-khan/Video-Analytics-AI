# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions (e.g., OpenCV binaries we don't need in repo)
*.so
*.pyd

# Virtual environment (ignore the whole venv folder with large OpenCV files)
venv/
env/
ENV/
env.bak/
venv.bak/

# Distribution / packaging (ignore build artifacts)
build/
dist/
*.egg-info/
.installed.cfg
*.egg

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports (if you add tests later)
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.pytest_cache/

# Flask-specific files
instance/
.webassets-cache

# Jupyter Notebook (if you use it for testing)
.ipynb_checkpoints

# IDE settings (e.g., PyCharm, VS Code)
.idea/
.vscode/

# Local configuration files (e.g., settings with sensitive data)
.env
local_settings.py

# Temporary files
*.log
*.bak

# Large file storage (if you use Git LFS later)
*.gitlfs*